// https://code.visualstudio.com/docs/editor/tasks
{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "npm",
      "script": "watch",
      // "problemMatcher": "$tsc-watch", // show tsc errors in problems panel but some stays forever
      "isBackground": true, // $tsc-watch needs isBackground true
      "presentation": {
        "reveal": "never"
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
